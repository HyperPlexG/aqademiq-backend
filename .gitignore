node_modules
dist
.env
keys/
*.log
.DS_Store
supabase/functions/_shared/prisma/
supabase/.temp/
.dart_tool/
.flutter-plugins-dependencies
cloud-sql-proxy.exe
/build/

# Firebase service-account key (secret — never commit)
*firebase-adminsdk*.json

# Service-account credentials. These are created in the repo root while wiring
# Vertex up (gcloud writes the key here) and a `git add -A` will happily stage
# them — GitHub's secret scanning caught exactly that once. Never committed.
ada-vertex.json
vertex.env
pk.txt
*-sa-key.json
